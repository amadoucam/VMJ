{% extends 'layout/layoutBO.html.twig' %}

{% block body %}
    <h1>Création d'un contact</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Créer" class="btn btn-success" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('newscontact_index') }}">Retour à la liste</a>
        </li>
    </ul>
{% endblock %}
